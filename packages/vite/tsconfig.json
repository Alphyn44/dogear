{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Runs inside the Vite dev server: Node only, no DOM.
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "tsup.config.ts"]
}
